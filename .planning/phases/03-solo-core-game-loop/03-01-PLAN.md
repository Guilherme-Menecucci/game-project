---
phase: 03-solo-core-game-loop
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/simulateTick.determinism.test.ts
  - packages/shared/src/__tests__/simulateTick.movement.test.ts
  - packages/shared/src/__tests__/spatialGrid.test.ts
  - packages/shared/src/__tests__/prng.test.ts
  - packages/shared/src/__tests__/spawn.test.ts
  - packages/shared/src/__tests__/ranged.behavior.test.ts
  - apps/game-server/package.json
  - apps/game-server/vitest.config.ts
  - apps/game-server/src/__tests__/setup.ts
  - apps/game-server/src/__tests__/soloRoom.anticheats.test.ts
  - apps/api/src/__tests__/auth/game-token.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-05
  - SEC-01
  - GAME-02
  - GAME-04
  - GAME-05
  - GAME-18

must_haves:
  truths:
    - "All 9 Wave 0 test files exist and fail RED on assertions (not import errors)"
    - "apps/game-server can run its own Vitest suite"
    - "PlayerInputSchema includes seq and tick fields (D-02)"
    - "pnpm test runs without import resolution errors from any package"
    - "ranged.behavior.test.ts covers keep-distance movement, enemy fire interval, and isEnemy:true projectile (GAME-05)"
  artifacts:
    - path: "packages/shared/src/__tests__/simulateTick.determinism.test.ts"
      provides: "Determinism test asserting byte-identical output for seeded simulateTick calls"
    - path: "packages/shared/src/__tests__/simulateTick.movement.test.ts"
      provides: "Movement test asserting consistent speed in all 8 directions"
    - path: "packages/shared/src/__tests__/spatialGrid.test.ts"
      provides: "Grid correctness + toroidal wrap + 300-enemy O(n) bench"
    - path: "packages/shared/src/__tests__/prng.test.ts"
      provides: "mulberry32 seeding + determinism assertions"
    - path: "packages/shared/src/__tests__/spawn.test.ts"
      provides: "Spawn curve + archetype mix + max-alive cap assertions (GAME-04, GAME-05)"
    - path: "packages/shared/src/__tests__/ranged.behavior.test.ts"
      provides: "Ranged archetype behavior: keep-distance movement, fire interval, isEnemy:true projectile (GAME-05)"
    - path: "apps/game-server/vitest.config.ts"
      provides: "Vitest config with resolve.conditions and ssr.resolve.conditions set"
    - path: "apps/game-server/src/__tests__/soloRoom.anticheats.test.ts"
      provides: "SC-4 fabricated-state-rejection test stub"
    - path: "apps/api/src/__tests__/auth/game-token.test.ts"
      provides: "D-05 game-token route test (401, 200, token payload, rate-limit 429)"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "simulateTick tests"
      via: "barrel re-export of simulateTick, UniformGrid, mulberry32, PlainGameState, spawnEnemies"
      pattern: "export.*simulateTick|UniformGrid|mulberry32|PlainGameState|spawnEnemies"
---

## Phase Goal

**As a** player, **I want to** click "Solo Run" and immediately enter a playable horde survival loop, **so that** I experience the core game chaos with zero friction.

<objective>
Create all Wave 0 test stubs from VALIDATION.md before any implementation runs. Tests must
import and fail on assertions — not crash on missing exports. This requires:
1. Expanding PlayerInputSchema with seq/tick (D-02)
2. Adding barrel re-exports for Phase 3 types/functions that tests import
3. Bootstrapping apps/game-server as a testable package
4. Writing 9 failing test stubs that describe the exact behavior Wave 1-3 plans must implement
   (includes ranged.behavior.test.ts for GAME-05 ranged archetype keep-distance + enemy fire)

Purpose: TDD RED phase for the entire Phase 3 non-retrofittable cluster. Every subsequent
plan must make at least one of these tests go GREEN. No plan ships new behavior without a
failing test from this wave already covering it.

Output: 12 files (6 shared tests, 1 game-server config, 1 game-server setup, 1 game-server test, 1 api test, 1 expanded schema, updated package.json)
</objective>

<execution_context>
@E:/Projects/gunpowder/game-project/.claude/get-shit-done/workflows/execute-plan.md
@E:/Projects/gunpowder/game-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/Projects/gunpowder/game-project/.planning/PROJECT.md
@E:/Projects/gunpowder/game-project/.planning/ROADMAP.md
@E:/Projects/gunpowder/game-project/.planning/STATE.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-CONTEXT.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-VALIDATION.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-RESEARCH.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-PATTERNS.md
</context>

<read_first>
- `packages/shared/src/index.ts` — existing exports; expand PlayerInputSchema with seq/tick per D-02; add barrel stubs for Phase 3 exports
- `apps/api/vitest.config.ts` — copy verbatim as template for apps/game-server/vitest.config.ts (both resolve.conditions AND ssr.resolve.conditions required)
- `apps/api/src/__tests__/setup.ts` — template for game-server setup.ts (different: no .env file, no Redis flush; only JWT_SECRET needed)
- `apps/api/src/__tests__/auth/guest.test.ts` — IP addressing pattern: use `10.5.X.X` prefix for game-token tests (other files use 10.2.X)
- `apps/api/src/lib/auth.ts` — existing signRegisteredJwt/signGuestJwt pattern; signGameJwt will be added in plan 03-03 but the test imports it here as a stub
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap apps/game-server and expand PlayerInputSchema</name>
  <files>
    apps/game-server/package.json
    apps/game-server/vitest.config.ts
    apps/game-server/src/__tests__/setup.ts
    packages/shared/src/index.ts
  </files>
  <behavior>
    - PlayerInputSchema gains two new fields: `seq: z.number().int().min(0)` (monotone sequence per D-02) and `tick: z.number().int().min(0)` (server tick reference per D-02)
    - PlayerInputSchema.safeParse({ moveVector: {x:1,y:0}, aimAngle:0, actionFlags:0, seq:1, tick:5 }) → success: true
    - PlayerInputSchema.safeParse({ moveVector: {x:1,y:0}, aimAngle:0, actionFlags:0 }) → STILL success (seq/tick optional for backward compat — see note in action)
    - apps/game-server vitest.config.ts resolves '@game/shared' via the 'source' condition in both browser and node environments
  </behavior>
  <action>
    Step 1 — apps/game-server/package.json: Replace the minimal shell with the full package needed
    for Phase 3. Set name to "@game/game-server", version "0.0.1", private true, type "module".
    Add scripts: dev runs "tsx watch src/server.ts", start runs "node dist/server.js", test runs "vitest run".

    Dependencies (exact versions per RESEARCH.md slopcheck):
    - colyseus@0.17.10
    - @colyseus/schema@4.0.26
    - @colyseus/tools@0.17.19
    - jsonwebtoken@9.0.2 (exact)
    - zod@4.0.5 (match workspace version — check apps/api/package.json for exact)
    - @game/shared@workspace:*

    devDependencies:
    - @colyseus/testing@0.17.11
    - @types/jsonwebtoken@9.0.9 (exact)
    - tsx@4.22.4 (exact — check apps/api package.json)
    - vitest@4.1.8 (exact — match workspace vitest version)
    - @vitest/coverage-v8@4.1.8 (exact)
    - typescript@5.8.3 (exact — match workspace)

    Step 2 — apps/game-server/vitest.config.ts: Copy apps/api/vitest.config.ts verbatim,
    then change setupFiles to ['./src/__tests__/setup.ts'] and remove fileParallelism (not
    needed — game-server tests don't share a database). Keep both resolve.conditions: ['source']
    and ssr.resolve.conditions: ['source'] exactly as-is.

    Step 3 — apps/game-server/src/__tests__/setup.ts: Create a minimal setup that only sets
    JWT_SECRET in process.env. Do NOT load a .env file (game-server has no .env in CI — it
    receives env vars directly). Do NOT flush Redis. Write:
    process.env['NODE_ENV'] = 'test'
    if (!process.env['JWT_SECRET']) { process.env['JWT_SECRET'] = 'test-secret-at-least-32-characters-long-here' }

    Step 4 — packages/shared/src/index.ts: Expand PlayerInputSchema. Add .optional() to both
    seq and tick so the schema remains backward-compatible with Phase 2 callers that don't
    yet supply these fields. Per D-02: these are anti-replay fields that will be required in
    Phase 4; optional here is the safe migration path.

    Also add barrel stubs for all Phase 3 types that test files will import. These stubs will
    be replaced by real implementations in plans 03-02 through 03-07. Add the following
    placeholder exports so TypeScript compiles and tests import without crashing:

    PlainPlayerState type: { id: string; x: number; y: number; hp: number; maxHp: number; level: number; xp: number; speed: number }
    PlainEnemyState type: { id: string; x: number; y: number; hp: number; maxHp: number; archetype: 'swarmer' | 'tank' | 'ranged'; speed: number; lastFireTick: number }
    PlainGemState type: { id: string; x: number; y: number; value: number }
    PlainProjectileState type: { id: string; x: number; y: number; vx: number; vy: number; ownerId: string; isEnemy: boolean; damage: number; lifetime: number }
    PlainGameState type: { tick: number; elapsedMs: number; players: Map of string to PlainPlayerState; enemies: Map; gems: Map; projectiles: Map; prngSeed: number }
    Prng type: { next: () => number; state: () => number }
    mulberry32 function stub: throws new Error('not implemented')
    UniformGrid class stub: insert/queryRadius/clear all throw new Error('not implemented')
    makeInitialState function stub: throws new Error('not implemented')
    simulateTick function stub: throws new Error('not implemented')
    spawnEnemies function stub: throws new Error('not implemented')

    All stubs throw "not implemented" at runtime — required for RED phase.
    Note: PlainEnemyState stub includes lastFireTick: number (added for GAME-05 ranged fire tracking).

    After writing: run pnpm --filter @game/shared build to verify no TypeScript errors in index.ts.
  </action>
  <verify>
    <automated>cd "E:/Projects/gunpowder/game-project" && pnpm --filter @game/shared build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - apps/game-server/package.json lists all required deps with exact versions
    - apps/game-server/vitest.config.ts has both resolve.conditions and ssr.resolve.conditions
    - PlayerInputSchema.parse succeeds with seq/tick present; succeeds without them (optional)
    - packages/shared/src/index.ts exports all Phase 3 stubs without TypeScript errors
    - PlainEnemyState stub includes lastFireTick: number
  </done>
  <acceptance_criteria>
    pnpm --filter @game/shared build exits 0. TypeScript compiler finds no errors in index.ts.
    The stubs throw "not implemented" at runtime (required for RED phase).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared package test stubs (determinism + movement + spatialGrid + prng + spawn + ranged behavior)</name>
  <files>
    packages/shared/src/__tests__/simulateTick.determinism.test.ts
    packages/shared/src/__tests__/simulateTick.movement.test.ts
    packages/shared/src/__tests__/spatialGrid.test.ts
    packages/shared/src/__tests__/prng.test.ts
    packages/shared/src/__tests__/spawn.test.ts
    packages/shared/src/__tests__/ranged.behavior.test.ts
  </files>
  <behavior>
    - All 6 test files import only from '@game/shared' barrel (not subpaths — subpath imports throw ERR_PACKAGE_PATH_NOT_EXPORTED)
    - All 6 tests fail with 'not implemented' errors (RED phase) — not module-not-found errors
    - determinism test: two calls to simulateTick with same seed produce JSON.stringify-identical results (SC-2)
    - movement test: player velocity magnitude is equal for all 8 directions (GAME-02 equal-speed constraint)
    - spatialGrid test: grid correctly returns entities within radius; correctly excludes entities outside radius; handles toroidal wrap at world boundary; 300-entity insert+queryRadius completes in under 5ms
    - prng test: mulberry32(42).next() returns same value on two identical seeds; seed state can be restored; all outputs are in [0, 1)
    - spawn test: spawn interval formula matches RESEARCH spec; archetype mix changes at correct elapsed-ms thresholds; max-alive cap prevents over-spawning (GAME-04, GAME-05)
    - ranged.behavior test: ranged archetype (archetype==='ranged') moves away from player when distance < KEEP_DISTANCE_INNER; fires isEnemy:true projectile when tick-lastFireTick >= ENEMY_FIRE_INTERVAL_TICKS; does not fire when interval not elapsed (GAME-05)
  </behavior>
  <action>
    Create 6 test files as failing stubs. Each imports from '@game/shared' (barrel only, no subpaths).

    simulateTick.determinism.test.ts — Tests SC-2 (byte-identical determinism) per RESEARCH Pattern 1:
    Import: simulateTick, makeInitialState, mulberry32, PlainGameState, PlayerInput from '@game/shared'.
    Test 1 'simulateTick is deterministic: byte-identical output for same seed':
    Call makeInitialState(12345) twice (state1, state2). Create prng with mulberry32(12345).
    Create empty inputs Map. Call simulateTick(state1, inputs, prng) to get result1.
    Call simulateTick(state2, inputs, mulberry32(12345)) to get result2.
    Assert: JSON.stringify(result1) === JSON.stringify(result2).
    Test 2 'simulateTick does not mutate input state':
    Call makeInitialState(99), freeze it (Object.freeze), call simulateTick — assert no throw.
    These tests will error with 'not implemented' at runtime (RED phase confirmed).

    simulateTick.movement.test.ts — Tests GAME-02 (8-direction movement, consistent speed):
    Import: simulateTick, makeInitialState, mulberry32, PlayerInput from '@game/shared'.
    The WORLD uses integer sub-units (1000 sub-units per game unit, per D-06/RESEARCH OQ-4).
    Player speed = 200 game units/sec = 200,000 sub-units/sec. At 20Hz: 10,000 sub-units/tick.
    Write 8 tests, one per direction (N, NE, E, SE, S, SW, W, NW):
    For each direction: create state with player at (2,000,000, 2,000,000) (center of 4096-unit world).
    Apply the appropriate unit moveVector. After one tick, assert displacement magnitude
    sqrt(dx^2 + dy^2) is within [9900, 10100] sub-units for all 8 directions.

    spatialGrid.test.ts — Tests GAME-18 (O(n) collision, toroidal wrap):
    Import: UniformGrid from '@game/shared'.
    Test 1 'queryRadius returns entities within radius':
    new UniformGrid(). insert('a', 100, 100). insert('b', 200, 200). insert('c', 5000, 5000).
    queryRadius(100, 100, 80) should contain 'a' but not 'c'.
    Test 2 'queryRadius handles toroidal wrap at world boundary':
    World size = 4,096,000 sub-units. Insert entity at (50, 50).
    queryRadius(4,095,950, 50, 200) should contain 'a' (wraps around x-boundary).
    Test 3 '300 entities: insert + queryRadius completes under 5ms':
    Insert 300 entities at random positions using mulberry32(1) for reproducibility.
    Time a queryRadius call at (2,000,000, 2,000,000, 500,000).
    Assert elapsed < 5ms using Date.now().

    prng.test.ts — Tests mulberry32 PRNG:
    Import: mulberry32 from '@game/shared'.
    Test 1 'same seed produces identical sequence': create two prngs with seed 42, assert
    first 10 next() calls produce identical sequences.
    Test 2 'all outputs are in [0, 1)': 1000 calls, assert all >= 0 and < 1.
    Test 3 'state() returns restorable seed': prng = mulberry32(42), call next() 5 times,
    save prng.state(), call next() 5 more times. Create prng2 = mulberry32(saved_state),
    assert first 5 next() calls from prng2 match the 5 calls from prng after save.

    spawn.test.ts — Tests GAME-04 (spawn curve) and GAME-05 (archetype mix):
    Import: spawnEnemies, makeInitialState, mulberry32 from '@game/shared'.

    Test 1 'spawn interval decreases with elapsed time':
    Create state with elapsedMs=0. Call spawnEnemies for enough ticks to trigger a spawn.
    Assert first spawn happens within first 2000ms of elapsed time (BASE_SPAWN_INTERVAL_MS=2000).
    Create second state with elapsedMs=50000. Assert spawn interval is shorter:
    Math.max(300, 2000 - 50000*0.02) = Math.max(300, 1000) = 1000ms.

    Test 2 'archetype mix: only swarmers before 60 seconds':
    Create state with elapsedMs=30000 (30s). Call spawnEnemies many times using a seeded prng.
    Assert all spawned enemies have archetype === 'swarmer'.

    Test 3 'archetype mix: tank and ranged appear after 60 seconds':
    Create state with elapsedMs=90000 (90s). Call spawnEnemies 100 times with seeded prng.
    Collect archetypes. Assert at least one 'tank' and at least one 'ranged' in the set.

    Test 4 'max-alive cap: no spawn when enemies.size >= cap':
    Create state with enemies.size = 300 (MAX_ENEMIES_CAP). Call spawnEnemies.
    Assert enemies.size remains 300 (no new enemy added).

    ranged.behavior.test.ts — Tests GAME-05 (ranged archetype keep-distance + enemy fire):
    Import: simulateTick, makeInitialState, mulberry32, PlainEnemyState from '@game/shared'.

    Constants to import (or inline as expected values in tests — will be exported from weapons.ts in plan 03-06):
    KEEP_DISTANCE_INNER = 120_000 sub-units
    ENEMY_FIRE_INTERVAL_TICKS = 20

    Test 1 'ranged archetype moves away from player when too close':
    Create state with one player at (2,000,000, 2,000,000) and one ranged enemy at
    (2,110,000, 2,000,000) — distance is 110,000 sub-units, inside KEEP_DISTANCE_INNER (120,000).
    Set enemy.archetype = 'ranged', enemy.lastFireTick = state.tick.
    Advance one non-fire tick (tick % ENEMY_FIRE_INTERVAL_TICKS !== 0).
    Assert: new enemy.x > 2,110,000 (enemy moved EAST, away from player — distance increased).
    This test will fail with 'not implemented' at runtime (RED — weapons.ts not yet created).

    Test 2 'ranged archetype fires isEnemy:true projectile at fire interval':
    Create state with one player at (2,000,000, 2,000,000) and one ranged enemy at
    (2,140,000, 2,000,000) — distance is 140,000 sub-units (inside fire band: 120k-160k).
    Set enemy.archetype = 'ranged', enemy.lastFireTick = 0.
    Advance to tick === ENEMY_FIRE_INTERVAL_TICKS (tick 20).
    Assert: state.projectiles.size >= 1.
    Assert: at least one projectile has isEnemy === true.
    This test will fail with 'not implemented' at runtime (RED).

    Test 3 'ranged archetype does NOT fire before interval elapses':
    Create state with ranged enemy. Set enemy.lastFireTick = state.tick (just fired).
    Advance one tick (tick = lastFireTick + 1, which is < ENEMY_FIRE_INTERVAL_TICKS).
    Assert: no isEnemy:true projectiles in state.projectiles.
    This test will fail with 'not implemented' at runtime (RED).

    After creating all 6 test files, run the suite to confirm RED (errors, not import failures):
    pnpm --filter @game/shared vitest run 2>&1 | tail -20
  </action>
  <verify>
    <automated>cd "E:/Projects/gunpowder/game-project" && pnpm --filter @game/shared vitest run 2>&1 | grep -E "(FAIL|not implemented|Error)" | head -20</automated>
  </verify>
  <done>
    All 6 test files exist. Running pnpm --filter @game/shared vitest run shows tests failing
    with "not implemented" errors or "throw new Error" — NOT with "Cannot find module" or
    TypeScript import errors. RED phase confirmed.
  </done>
  <acceptance_criteria>
    pnpm --filter @game/shared vitest run output contains "not implemented" or explicit assertion
    failures — no "ERR_PACKAGE_PATH_NOT_EXPORTED", no "Cannot find module '@game/shared'", no
    TypeScript compilation errors in test files. Test count is at least 19 (existing + 3 from ranged.behavior.test.ts).
    grep "isEnemy.*true\|isEnemy === true" packages/shared/src/__tests__/ranged.behavior.test.ts returns a match.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create game-server and API test stubs (soloRoom anticheats + game-token route)</name>
  <files>
    apps/game-server/src/__tests__/soloRoom.anticheats.test.ts
    apps/api/src/__tests__/auth/game-token.test.ts
  </files>
  <behavior>
    - soloRoom.anticheats.test.ts: imports from @colyseus/testing; verifies room rejects fabricated state mutations (SC-4); tests room lifecycle join+tick+leave (TEST-05); fails RED because SoloRoom doesn't exist yet
    - game-token.test.ts: 4 test cases covering 401 (no cookie), 200 (valid cookie returns JWT), token payload has {userId, type:'game'}, rate-limit 429 on 11th request; uses 10.5.X.X IP prefix
    - Both files import only from modules that will exist by Wave 1 (their imports are the spec for what Wave 1 must create)
  </behavior>
  <action>
    Install game-server dependencies first (required before vitest can run):
    Run pnpm install from the workspace root to install the deps listed in the updated
    apps/game-server/package.json. This is required for the test runner to find vitest.

    Create apps/game-server/src/__tests__/soloRoom.anticheats.test.ts:
    Import from '@colyseus/testing': { ColyseusTestServer, boot }
    Import from '../app.config.js': { appConfig } (this module will be created in plan 03-04)
    Import from '@game/shared': { PlainGameState, PlayerInput }

    Test suite 'SoloRoom anti-cheat validation (SC-4, TEST-05)':

    Test 1 'rejects message with unknown type':
    boot(appConfig). createRoom('solo_room'). connectTo(room).
    client.send('hack_state', { x: 999999 }).
    await waitForNextSimulationTick(room).
    Assert room.state is unchanged from initial state (no hack_state handler exists → message silently dropped, state not mutated).

    Test 2 'rejects input with invalid schema (missing seq field causes no state change)':
    Connect client. Send message 'input' with payload { moveVector: {x:99, y:0} } (missing seq/tick/aimAngle/actionFlags).
    Wait for tick. Assert player position unchanged (safeParse fails → input ignored).

    Test 3 'accepts valid input and advances game state':
    Connect client with valid auth token (mock or skip auth in test env).
    Send valid input per PlayerInputSchema.
    waitForNextSimulationTick.
    Assert room.state.tick > 0.

    Test 4 'client cannot directly mutate server state':
    Attempt to call room.state.players.set('fakeid', ...) from client-side.
    This must throw or be a no-op — Colyseus Schema types are server-side only; clients receive
    read-only patches. Assert that schema mutation from a client reference throws TypeError.

    NOTE: These tests will fail RED because appConfig / SoloRoom don't exist yet. That is correct
    for Wave 0. The import path '../app.config.js' is the contract that plan 03-04 must satisfy.

    Create apps/api/src/__tests__/auth/game-token.test.ts:
    Read apps/api/src/__tests__/auth/guest.test.ts first for the exact pattern (app setup,
    cookie injection, teardown). Then write:

    Import: { buildApp } from '../../app.config.js', { signGuestJwt, signRegisteredJwt } from '../../lib/auth.js'
    (signGameJwt will be added in plan 03-03; the test imports it as a future export)

    Use IP prefix '10.5' for all test request IPs (distinct from 10.2.X used by other auth tests
    to prevent rate-limit counter bleed — per STATE.md decision on per-PID IP generation).

    Test suite 'GET /auth/game-token':

    Test 1 '401 when no session cookie present':
    GET /auth/game-token with no cookies → assert 401.

    Test 2 '200 valid guest session returns game JWT':
    Set cookie header with a valid guest session JWT (signed using signGuestJwt).
    GET /auth/game-token → assert 200, body has { token: string }.

    Test 3 'returned token has correct payload':
    Decode the returned token (without verifying signature — just JSON.parse of base64 payload).
    Assert payload contains { userId: string, type: 'game' }.
    Assert exp is within 5 minutes of now (300 seconds).

    Test 4 'rate limit: 429 on 11th request within window':
    Make 10 successful GET requests. The 11th should return 429.
    Use IP '10.5.1.1' with x-forwarded-for header for all requests.

    These tests will fail RED when run because GET /auth/game-token route doesn't exist yet.
    Confirm the route is 404 after writing the tests.
  </action>
  <verify>
    <automated>cd "E:/Projects/gunpowder/game-project" && pnpm install 2>&1 | tail -3 && pnpm --filter @game/api vitest run game-token 2>&1 | grep -E "(FAIL|404|not found|Error)" | head -10</automated>
  </verify>
  <done>
    - apps/game-server/src/__tests__/soloRoom.anticheats.test.ts exists (will fail at import of app.config.js)
    - apps/api/src/__tests__/auth/game-token.test.ts exists and fails with 404 or similar (route not created yet)
    - pnpm install completes successfully (game-server deps installed)
  </done>
  <acceptance_criteria>
    pnpm install exits 0. Both test files exist with correct imports.
    pnpm --filter @game/api vitest run game-token fails on the actual assertion (404 response
    or route not found) — not on TypeScript errors or missing imports from @game/shared barrel.
    soloRoom.anticheats.test.ts exists; its RED failure on missing app.config.js is acceptable
    (Wave 0 RED — appConfig is created in plan 03-04).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → shared package | Tests import stubs via barrel; subpath import bypass would expose internal module paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering | npm install (colyseus, @colyseus/schema, @colyseus/tools, @colyseus/testing) | mitigate | RESEARCH.md slopcheck confirmed [OK] for all 5 Colyseus packages; jsonwebtoken@9.0.2 [OK] |
| T-3-04 | Tampering | PlayerInputSchema | mitigate | seq/tick fields added (D-02); Wave 0 test stub asserts safeParse rejects missing required fields |
</threat_model>

<verification>
Run from workspace root after all tasks complete:

pnpm --filter @game/shared vitest run

Expected: tests run and fail with "not implemented" — not import errors.

pnpm --filter @game/api vitest run game-token

Expected: test fails with 404 (route missing) — not with TypeScript errors.
</verification>

<success_criteria>
- All 9 Wave 0 files from VALIDATION.md exist (including spawn.test.ts and ranged.behavior.test.ts)
- apps/game-server is a valid pnpm workspace package with vitest runnable
- All test files import from @game/shared barrel (no subpaths) — import resolution works
- Tests fail RED on assertions/missing routes, not on missing modules
- PlayerInputSchema accepts seq and tick (optional) — backward compat preserved
- PlainEnemyState stub includes lastFireTick: number (GAME-05)
- ranged.behavior.test.ts covers keep-distance movement, fire interval, isEnemy:true projectile (GAME-05)
- pnpm install exits 0 at workspace root
</success_criteria>

<output>
Create `.planning/phases/03-solo-core-game-loop/03-01-SUMMARY.md` when done.
</output>
