---
phase: 03-solo-core-game-loop
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - packages/shared/src/state.ts
  - packages/shared/src/prng.ts
  - packages/shared/src/spatialGrid.ts
  - packages/shared/src/simulateTick.ts
  - packages/shared/src/spawn.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-04
  - GAME-05
  - GAME-18
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "simulateTick is a pure function with no side effects — same inputs always produce identical outputs (SC-2)"
    - "Player moves in all 8 directions at consistent speed using integer sub-units (D-06, GAME-02)"
    - "mulberry32 PRNG produces deterministic sequences and never calls Math.random in the tick path (D-03)"
    - "UniformGrid resolves 300-entity neighborhoods in O(n) with toroidal wrap (GAME-18)"
    - "Enemies spawn per the escalating curve: swarmers 0-60s, mixed types after 60s (GAME-04, GAME-05)"
    - "Max-alive cap (300) prevents unbounded spawning (GAME-04)"
    - "All simulateTick, spawn, spatialGrid, and prng tests pass GREEN"
  artifacts:
    - path: "packages/shared/src/state.ts"
      provides: "PlainGameState and related entity types; makeInitialState(seed)"
      exports: ["PlainGameState", "PlainPlayerState", "PlainEnemyState", "PlainGemState", "PlainProjectileState", "makeInitialState"]
    - path: "packages/shared/src/prng.ts"
      provides: "mulberry32 seeded PRNG — 6-line implementation, Prng interface"
      exports: ["mulberry32", "Prng"]
    - path: "packages/shared/src/spatialGrid.ts"
      provides: "UniformGrid class: insert, queryRadius (toroidal), clear"
      exports: ["UniformGrid", "CELL_SIZE", "WORLD_W", "WORLD_H"]
    - path: "packages/shared/src/simulateTick.ts"
      provides: "Pure simulateTick function — player movement, enemy movement; TICK_SEC constant"
      exports: ["simulateTick", "TICK_SEC", "WORLD_W", "WORLD_H", "SPEED_SUBUNITS"]
    - path: "packages/shared/src/spawn.ts"
      provides: "spawnEnemies(state, prng) — enemy spawn curve + archetype mix + max-alive cap"
      exports: ["spawnEnemies", "MAX_ENEMIES_CAP", "BASE_SPAWN_INTERVAL_MS", "MIN_SPAWN_INTERVAL_MS"]
  key_links:
    - from: "packages/shared/src/simulateTick.ts"
      to: "packages/shared/src/spatialGrid.ts"
      via: "imports WORLD_W, WORLD_H for toroidal movement wrap"
      pattern: "import.*WORLD_W.*spatialGrid"
    - from: "packages/shared/src/simulateTick.ts"
      to: "packages/shared/src/spawn.ts"
      via: "calls spawnEnemies each tick"
      pattern: "spawnEnemies"
    - from: "packages/shared/src/index.ts"
      to: "all above modules"
      via: "barrel re-exports replacing Wave 0 stubs"
      pattern: "export.*from.*state|prng|spatialGrid|simulateTick|spawn"
---

## Phase Goal

**As a** player, **I want to** click "Solo Run" and immediately enter a playable horde survival loop, **so that** I experience the core game chaos with zero friction.

<objective>
Implement the shared game simulation core — the non-retrofittable cluster of pure functions that
the Colyseus Room (plan 03-04) and the Phaser client (plan 03-07) will both depend on.

This is Milestone 1a: "The sim can tick deterministically." After this plan, the Wave 0 tests
for simulateTick, spawn, spatialGrid, and prng all go GREEN. The Colyseus Room (plan 03-04) can be
created as a thin adapter over this pure core.

Purpose: Lock in the non-retrofittable architectural decisions (D-03, D-06, D-07):
- Integer sub-units: all positions stored as integers (1000 sub-units per game unit)
- PRNG: mulberry32, seeded, deterministic, NEVER Math.random in tick path
- Pure function: simulateTick takes state + inputs + prng, returns new state, no side effects
- Spawn curve: deterministic archetype mix, escalating cap, seeded PRNG-driven

Output: 6 files in packages/shared/src/ implementing the complete game simulation minus weapons/collision (plan 03-06 handles those)
</objective>

<execution_context>
@E:/Projects/gunpowder/game-project/.claude/get-shit-done/workflows/execute-plan.md
@E:/Projects/gunpowder/game-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/Projects/gunpowder/game-project/.planning/PROJECT.md
@E:/Projects/gunpowder/game-project/.planning/ROADMAP.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-CONTEXT.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-RESEARCH.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-PATTERNS.md
@E:/Projects/gunpowder/game-project/.planning/phases/03-solo-core-game-loop/03-01-SUMMARY.md
</context>

<read_first>
- `packages/shared/src/index.ts` — current barrel exports (stubs from plan 03-01); replace stubs with real re-exports after each implementation file is created
- `packages/shared/src/__tests__/simulateTick.determinism.test.ts` — read before implementing; must go GREEN after this plan
- `packages/shared/src/__tests__/simulateTick.movement.test.ts` — read before implementing; diagonal speed tolerance [9900, 10100] sub-units magnitude
- `packages/shared/src/__tests__/spatialGrid.test.ts` — read before implementing; toroidal wrap test requires grid to use WORLD_W/WORLD_H boundaries
- `packages/shared/src/__tests__/prng.test.ts` — read before implementing; state() restore test requires saved seed to reconstruct identical future sequence
- `packages/shared/src/__tests__/spawn.test.ts` — read before implementing; must go GREEN with exact interval formula and archetype thresholds
- 03-RESEARCH.md Pattern 1 (simulateTick structure), Pattern 6 (UniformGrid), Pattern 9 (mulberry32)
- 03-RESEARCH.md Open Question 4 (sub-units): positions stored as 1000x integer, speed = 200,000 sub-units/sec = 10,000 sub-units/tick
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement prng.ts, spatialGrid.ts, and state.ts</name>
  <files>
    packages/shared/src/prng.ts
    packages/shared/src/spatialGrid.ts
    packages/shared/src/state.ts
    packages/shared/src/index.ts
  </files>
  <behavior>
    - mulberry32(seed).next() returns float in [0, 1), same seed always produces same sequence
    - mulberry32(seed).state() returns current internal state for PRNG restore
    - UniformGrid(cellSize, worldW, worldH): insert(id, x, y) buckets entity; queryRadius(x, y, r) returns all ids within r using toroidal arithmetic; clear() empties all buckets
    - UniformGrid cell size = 64,000 sub-units (64 game units x 1000 sub-units), world = 4,096,000 x 4,096,000 sub-units
    - makeInitialState(seed) returns a PlainGameState with empty maps, tick=0, elapsedMs=0, prngSeed=seed
    - All prng and spatialGrid tests go GREEN after this task
  </behavior>
  <action>
    Create packages/shared/src/prng.ts:
    Implement mulberry32 per RESEARCH Pattern 9. The function takes an initial seed (uint32)
    and returns a Prng object. Internal mutable variable s holds the current state.
    next() advances s using the mulberry32 algorithm:
      s += 0x6D2B79F5
      let z = s
      z = (z ^ (z >>> 15)) * (z | 1)
      z ^= z + (z ^ (z >>> 7)) * (z | 61)
      return ((z ^ (z >>> 14)) >>> 0) / 0x100000000
    state() returns s (>>> 0 for uint32 safety).
    Export: Prng interface { next: () => number; state: () => number } and mulberry32 function.

    CRITICAL: NEVER call Math.random() anywhere in this file or any game simulation file.
    The tick path must be 100% deterministic. Any Math.random() call would make SC-2
    (byte-identical determinism) impossible to satisfy.

    Create packages/shared/src/spatialGrid.ts:
    Export constants: CELL_SIZE = 64_000 (64 game units x 1000 sub-units), WORLD_W = 4_096_000,
    WORLD_H = 4_096_000.

    Export class UniformGrid:
    Constructor: takes no arguments; uses CELL_SIZE, WORLD_W, WORLD_H constants.
    cols = Math.ceil(WORLD_W / CELL_SIZE) = 64. rows = Math.ceil(WORLD_H / CELL_SIZE) = 64.
    Internal storage: Map of number key to Map of string id to {x, y} (stores positions alongside ids for accurate distance re-check in queryRadius).

    insert(id: string, x: number, y: number): compute cellX = Math.floor(x / CELL_SIZE) % 64,
    cellY = Math.floor(y / CELL_SIZE) % 64 (modulo handles exact boundary). Compute key =
    cellX * 64 + cellY. Get or create inner Map at key. Add id with {x, y}.

    queryRadius(cx: number, cy: number, r: number): string[] result. Compute cell radius
    (Math.ceil(r / CELL_SIZE) + 1 extra for boundary safety). Iterate candidate cells using
    toroidal modular arithmetic: for dx in [-cellR, cellR] for dy in [-cellR, cellR]:
    ncx = ((Math.floor(cx / CELL_SIZE) + dx) % 64 + 64) % 64 (toroidal)
    ncy = ((Math.floor(cy / CELL_SIZE) + dy) % 64 + 64) % 64
    For each entity in that cell: compute toroidal distance using:
    let adx = Math.abs(ex - cx); if adx > WORLD_W/2: adx = WORLD_W - adx
    let ady = Math.abs(ey - cy); if ady > WORLD_H/2: ady = WORLD_H - ady
    if adx*adx + ady*ady <= r*r: add id to result.

    clear(): inner map cleared entirely (cells.clear()).

    Create packages/shared/src/state.ts:
    Export all entity types and PlainGameState (replacing the inline stubs in index.ts):
    PlainPlayerState: { id: string; x: number; y: number; hp: number; maxHp: number; level: number; xp: number; speed: number }
    PlainEnemyState: { id: string; x: number; y: number; hp: number; maxHp: number; archetype: 'swarmer' | 'tank' | 'ranged'; speed: number; lastFireTick: number }
    PlainGemState: { id: string; x: number; y: number; value: number }
    PlainProjectileState: { id: string; x: number; y: number; vx: number; vy: number; ownerId: string; isEnemy: boolean; damage: number; lifetime: number }
    PlainGameState: { tick: number; elapsedMs: number; players: Map of string to PlainPlayerState; enemies: Map of string to PlainEnemyState; gems: Map of string to PlainGemState; projectiles: Map of string to PlainProjectileState; prngSeed: number }

    makeInitialState(seed: number): PlainGameState — returns state with empty Maps, tick=0,
    elapsedMs=0, prngSeed=seed.

    Update packages/shared/src/index.ts:
    Remove the inline stub type definitions and throw-stubs for prng, spatialGrid, and state.
    Add real re-exports: export * from './state.js'; export * from './prng.js'; export * from './spatialGrid.js'
    Keep simulateTick and spawnEnemies stubs until plan 03-02 Task 2 implements them.
  </action>
  <verify>
    <automated>cd "E:/Projects/gunpowder/game-project" && pnpm --filter @game/shared vitest run prng spatialGrid 2>&1 | tail -20</automated>
  </verify>
  <done>
    prng.test.ts and spatialGrid.test.ts both pass GREEN. mulberry32 returns consistent sequences.
    UniformGrid.queryRadius handles toroidal boundary. 300-entity bench under 5ms.
    pnpm --filter @game/shared build exits 0.
  </done>
  <acceptance_criteria>
    pnpm --filter @game/shared vitest run prng spatialGrid exits 0 with all tests passing.
    pnpm --filter @game/shared build exits 0 (no TypeScript errors). Math.random appears
    zero times in prng.ts and spatialGrid.ts (grep confirms).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement simulateTick.ts and spawn.ts — make all 5 shared tests GREEN</name>
  <files>
    packages/shared/src/simulateTick.ts
    packages/shared/src/spawn.ts
    packages/shared/src/index.ts
  </files>
  <behavior>
    - simulateTick(state, inputs, prng) returns a NEW PlainGameState (never mutates input)
    - TICK_SEC = 1/20 (0.05 seconds per tick) — NEVER use dt from setSimulationInterval
    - Player speed = 200,000 sub-units/tick (10,000 sub-units/tick at 20Hz)
    - Diagonal movement: normalize moveVector to integer using Math.round(dx * scale / mag); magnitude within [9900, 10100] sub-units for all 8 directions
    - Toroidal world wrap: x = ((x % WORLD_W) + WORLD_W) % WORLD_W
    - spawnEnemies: interval = max(300, 2000 - elapsed_ms * 0.02), cap = min(300, 20 + floor(elapsed_sec/5)*10)
    - Enemy archetype mix: 0-60s swarmers only; 60-120s 70/20/10 swarmer/tank/ranged; 120s+ 50/30/20
    - All 5 shared tests pass GREEN: determinism, movement, spatialGrid, prng, spawn
  </behavior>
  <action>
    Create packages/shared/src/spawn.ts:
    Export constants:
    - BASE_SPAWN_INTERVAL_MS = 2000
    - MIN_SPAWN_INTERVAL_MS = 300
    - BASE_MAX_ENEMIES = 20
    - MAX_ENEMIES_CAP = 300

    Export function spawnEnemies(state: PlainGameState, prng: Prng): PlainGameState
    — returns new state with enemies added (do not mutate input state).

    Spawn interval formula (exact match for spawn.test.ts Test 1):
    spawnIntervalMs = Math.max(MIN_SPAWN_INTERVAL_MS, BASE_SPAWN_INTERVAL_MS - state.elapsedMs * 0.02)
    Spawn decision: only spawn if (state.tick % Math.round(spawnIntervalMs / (TICK_SEC * 1000))) === 0
    Max alive: Math.min(MAX_ENEMIES_CAP, BASE_MAX_ENEMIES + Math.floor(state.elapsedMs / 5000) * 10)
    If enemies.size >= maxAlive — return state unchanged (no spawn).

    Archetype selection based on elapsedMs (exact thresholds for spawn.test.ts Test 2 and 3):
    - 0..60000ms: swarmer 100% (roll = prng.next(), always swarmer)
    - 60001..120000ms: roll = prng.next(); if roll < 0.7: swarmer; elif roll < 0.9: tank; else: ranged
    - 120001ms+: roll = prng.next(); if roll < 0.5: swarmer; elif roll < 0.8: tank; else: ranged
    Never call Math.random() — prng parameter only.

    Enemy spawn position: spawn at distance 600,000 sub-units from a random player.
    angle = prng.next() * 2 * Math.PI.
    dx = Math.round(Math.cos(angle) * 600_000), dy = Math.round(Math.sin(angle) * 600_000).
    If no players: spawn at (WORLD_W/2, WORLD_H/2) with prng-seeded offset.
    Apply toroidal wrap.

    Enemy stats per archetype:
    - swarmer: hp=1, maxHp=1, speed=7_500 (150 units/sec * 1000 / 20 ticks = 7500 sub-units/tick)
    - tank: hp=10, maxHp=10, speed=3_000 (60 units/sec * 1000 / 20)
    - ranged: hp=3, maxHp=3, speed=4_500 (90 units/sec * 1000 / 20)
    Note: speed is sub-units PER TICK (not per second) — consistent with player's SPEED_SUBUNITS = 10_000.

    XP per archetype (used in plan 03-06 gem drops): swarmer=1, tank=3, ranged=2.
    Store as exported constant: XP_PER_ARCHETYPE: Record<'swarmer'|'tank'|'ranged', number> = { swarmer: 1, tank: 3, ranged: 2 }

    Generate unique enemy id using state.tick + enemy count: `e${state.tick}_${state.enemies.size}`.

    Create packages/shared/src/simulateTick.ts:
    Export: TICK_SEC = 1/20, SPEED_SUBUNITS = 10_000
    (WORLD_W and WORLD_H are exported from spatialGrid.ts — import them from there, do not redeclare)

    Export function simulateTick(state: PlainGameState, inputs: Map of string to PlayerInput, prng: Prng): PlainGameState

    Implementation:
    1. Deep-clone state at the start (structuredClone or manual Map copying — NO mutation of input)
       Cloning Maps: new Map([...state.players.entries()]) is acceptable for small maps;
       if players/enemies/gems/projectiles can have nested mutable objects, clone their values too.
    2. Advance tick counter: newState.tick = state.tick + 1
    3. Advance elapsedMs: newState.elapsedMs = state.elapsedMs + TICK_SEC * 1000

    Player movement per input:
    For each player in state.players:
    - Get input from inputs.get(playerId) — if none, use moveVector {x:0, y:0}
    - Validate input with PlayerInputSchema.safeParse — if fails, skip movement
    - If moveVector is zero-length (x===0 && y===0): no movement
    - Normalize for diagonal: mag = Math.sqrt(dx*dx + dy*dy)
      vx = Math.round(dx * SPEED_SUBUNITS / mag)
      vy = Math.round(dy * SPEED_SUBUNITS / mag)
      Note: Math.round ensures integer result; at (1,1)/sqrt(2) → 7071
    - Apply toroidal wrap: x = ((newX % WORLD_W) + WORLD_W) % WORLD_W

    Enemy movement toward nearest player (placeholder — will be replaced by applyEnemyAI in plan 03-06):
    For each enemy: find nearest player using toroidal distance.
    Toroidal distance: adx = Math.abs(ex-px); if adx > WORLD_W/2: adx = WORLD_W - adx; same for y.
    Direction toward player (accounting for toroidal shortest path):
    dx = px - ex; if Math.abs(dx) > WORLD_W/2: dx = dx > 0 ? dx - WORLD_W : dx + WORLD_W (same for dy)
    mag = Math.sqrt(dx*dx + dy*dy); vx = Math.round(dx * enemy.speed / mag); vy = Math.round(dy * enemy.speed / mag)
    Apply toroidal wrap.
    IMPORTANT: This inline enemy movement block moves ALL enemies (swarmer + tank + ranged) directly
    toward the nearest player at the enemy.speed rate. Plan 03-06 Task 2 will remove this entire block
    and replace it with a single call: newState = applyEnemyAI(newState), which handles all three
    archetypes including the ranged keep-distance behavior. Do NOT skip this step when wiring 03-06 —
    leaving both the inline block and applyEnemyAI active would cause double-movement (2x speed).

    Call spawnEnemies(newState, prng) to possibly add enemies.

    Return newState.

    CRITICAL: TICK_SEC = 1/20 is a compile-time constant. The Colyseus Room's
    setSimulationInterval callback receives _dt (wall-clock delta) which varies due to jitter.
    Plan 03-04 SoloRoom passes TICK_SEC to simulateTick, never _dt.
    Never use _dt in any simulation logic.

    Update packages/shared/src/index.ts:
    Replace stub exports for simulateTick and spawnEnemies with real re-exports:
    export * from './simulateTick.js'; export * from './spawn.ts'
    Remove any remaining inline stubs (ensure no duplicate exports).

    After all implementations, run the full test suite:
    pnpm --filter @game/shared vitest run

    Expected: all 5 test files pass GREEN (prng, spatialGrid, determinism, movement, spawn).
    If determinism test fails: check for any Math.random() call or non-deterministic Map iteration order.
    If spawn test 3 fails (no tank/ranged): verify the archetype probability roll at elapsedMs=90000 uses prng.next() not Math.random() and probability thresholds match the spec above.
    If movement tolerance test fails: verify SPEED_SUBUNITS = 10_000, not 10_000/TICK_SEC or other value.
  </action>
  <verify>
    <automated>cd "E:/Projects/gunpowder/game-project" && pnpm --filter @game/shared vitest run 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 5 shared package test suites pass GREEN:
    - simulateTick.determinism.test.ts: JSON.stringify identical outputs
    - simulateTick.movement.test.ts: all 8 directions within [9900, 10100] sub-unit magnitude
    - spatialGrid.test.ts: all grid tests pass including toroidal wrap
    - prng.test.ts: all prng tests pass including state restore
    - spawn.test.ts: interval formula, archetype mix at both thresholds, max-alive cap all verified
    pnpm --filter @game/shared build exits 0.
  </done>
  <acceptance_criteria>
    pnpm --filter @game/shared vitest run exits 0 with all tests passing GREEN. Test count >= 20.
    grep -r "Math.random" packages/shared/src/ returns zero matches.
    pnpm --filter @game/shared build exits 0 (TypeScript clean).
    grep "TICK_SEC" packages/shared/src/simulateTick.ts returns a match (constant defined).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client input → simulateTick | PlayerInput validated inside simulateTick via PlayerInputSchema.safeParse before any movement applied |
| Client moveVector magnitude | Unnormalized client vectors clamped to unit-sphere by normalization before applying SPEED_SUBUNITS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | simulateTick player position | mitigate | Position computed server-side from validated input; client never sends position, only moveVector |
| T-3-02 | Tampering | simulateTick state mutation | mitigate | Input state deep-cloned at start of tick; structuredClone ensures input is never mutated |
| T-3-SC | Tampering | npm install | accept | All packages slopcheck [OK] in RESEARCH.md; no new installs in this plan |
</threat_model>

<verification>
Run from workspace root after all tasks complete:

pnpm --filter @game/shared vitest run

All 5 test suites (determinism, movement, spatialGrid, prng, spawn) must exit 0 GREEN.

grep -r "Math.random" packages/shared/src/

Must return zero matches.
</verification>

<success_criteria>
- All Wave 0 shared package tests pass GREEN (SC-2 determinism verified by test)
- simulateTick is a pure function: no side effects, no Math.random, TICK_SEC constant used
- Player moves at consistent speed in all 8 directions (GAME-02)
- Enemy spawn curve implemented: escalating from 20 to 300 max, archetype mix by elapsed time (GAME-04, GAME-05)
- Max-alive cap (300) enforced by automated test
- pnpm --filter @game/shared build exits 0 (TypeScript clean)
</success_criteria>

<output>
Create `.planning/phases/03-solo-core-game-loop/03-02-SUMMARY.md` when done.
</output>
