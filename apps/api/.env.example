DATABASE_URL=postgresql://gamedev:gamedev@localhost:5432/gamedev
DATABASE_URL_TEST=postgresql://gamedev:gamedev@localhost:5432/gamedev_test
REDIS_URL=redis://127.0.0.1:6379
JWT_SECRET=<generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))">
# Game token (GET /auth/game-token): 5-min JWT for Colyseus room auth (D-05)
# Uses same JWT_SECRET as session tokens — Colyseus validates via jsonwebtoken with this secret
# See apps/game-server/src/lib/gameToken.ts for the verification side (plan 03-04)
RESEND_API_KEY=<from resend.com dashboard — D-08>
NODE_ENV=development
BASE_URL=http://localhost:5173
